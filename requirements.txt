python-telegram-bot==20.3  # Replace with the latest compatible version
requests==2.31.0           # HTTP library for checking username availability
asyncio                    # Asynchronous framework (built-in for Python 3.7+)
